name: ci

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build-and-test:
    name: ocaml ${{ matrix.ocaml }} + lua ${{ matrix.lua }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        ocaml:
          # 4.14 is the oldest version js_of_ocaml-compiler 6.2 still
          # supports.  test/behavioral/effects.ml is skipped on 4.x
          # (the Effect module is 5.0+); everything else runs.
          - "4.14.x"
          - "5.1.x"
          - "5.2.x"
          - "5.3.x"
          - "5.4.x"
        lua:
          - "luajit"
          - "lua5.1"
          - "lua5.4"

    steps:
      - uses: actions/checkout@v4

      - name: install Lua/LuaJIT
        run: |
          sudo apt-get update
          sudo apt-get install -y luajit lua5.1 lua5.4

      - uses: ocaml/setup-ocaml@v3
        with:
          ocaml-compiler: ${{ matrix.ocaml }}

      - name: install opam deps
        run: opam install . --deps-only --with-test --yes

      - name: build
        run: opam exec -- dune build

      - name: main test suite
        run: opam exec -- env LUA=${{ matrix.lua }} bash test/run_all.sh

      - name: behavioral suite
        run: opam exec -- env LUA=${{ matrix.lua }} bash test/behavioral/run.sh
